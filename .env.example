# Environment Variables Example for Next_web

# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Authentication (Auth.js)
AUTH_SECRET="your-auth-secret-here"

# Create.xyz Integration (Required if using sandbox features)
NEXT_PUBLIC_PROJECT_GROUP_ID="your-project-group-id"
NEXT_PUBLIC_CREATE_BASE_URL="https://www.create.xyz"
NEXT_PUBLIC_CREATE_API_BASE_URL="https://api.create.xyz"

# CORS configuration
CORS_ORIGINS="http://localhost:4000,https://your-domain.com"
